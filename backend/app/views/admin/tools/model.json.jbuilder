json.message nil
json.data do
  json.amass ['config_value']
  json.nuclei ['config_value']
  json.dehashed %w[user api_key]
  json.c99 ['api_key']
  json.whoxy ['api_key']
  json.slack ['webhook']
  json.hunt3r_token ['api_key']
  json.interactsh %w[url api_key]
end
